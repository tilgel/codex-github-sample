# Codex GitHub Sample

A tiny static web app created by Codex to demonstrate a local project that can be committed and published to GitHub.

## Run locally

Open `index.html` in a browser, or serve the folder with any static file server.

```sh
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
